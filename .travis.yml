language: java
jdk:
  - oraclejdk8
sudo: required
env:
  - RASCAL_JAR="$HOME/rascal/rascal-shell-unstable.jar"
script:
  - bin/get_rascal #only going to download if it is not cached
  - bin/build
  - mvn clean package
  - bin/glagol test
  - ls
  #- ./coverage report
cache:
  directories:
  - $HOME/rascal
  - $HOME/.m2
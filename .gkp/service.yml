apiVersion: v1
kind: Service
metadata:
  name: glagol-server-svc
  labels:
    app: glagol-server
spec:
  type: NodePort
  ports:
  - port: 51151
    nodePort: 30001
    protocol: TCP
  selector:
    app: glagol-server